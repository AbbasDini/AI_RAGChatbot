# راهنمای کامل TTS آولای (AvalAI)

## 🎯 **معرفی TTS آولای**

سیستم تبدیل متن به صدا آولای (AvalAI) یک سرویس ایرانی با کیفیت بالا است که از مدل‌های پیشرفته LLM استفاده می‌کند.

## 🚀 **ویژگی‌های کلیدی**

### ✅ **مدل‌های پشتیبانی شده**
- `tts-1` - مدل پایه با کیفیت خوب
- `tts-1-hd` - مدل با کیفیت بالا
- `gpt-4o-mini-tts` - مدل پیشرفته GPT-4
- `gemini-2.5-pro-preview-tts` - مدل Gemini با کیفیت عالی
- `gemini-2.5-flash-preview-tts` - مدل Gemini سریع

### 🎵 **صداهای موجود**
- `alloy` - مرد، طبیعی
- `ash` - مرد، گرم
- `ballad` - زن، ملایم
- `coral` - زن، روشن
- `echo` - مرد، گرم
- `fable` - مرد، جوان
- `onyx` - مرد، بالغ
- `nova` - زن، طبیعی
- `sage` - مرد، خردمند
- `shimmer` - زن، جوان
- `verse` - زن، شاعرانه

### 🌍 **پشتیبانی از زبان‌ها**
- **فارسی** - پشتیبانی کامل
- **انگلیسی** - پشتیبانی کامل
- **عربی** - پشتیبانی خوب
- **سایر زبان‌ها** - پشتیبانی محدود

## 🔧 **نحوه راه‌اندازی**

### 1. تنظیم کلید API
در فایل `.env`:
```env
AVALAI_API_KEY=your_avalai_api_key_here
AVALAI_BASE_URL=https://api.avalai.ir/v1
```

### 2. نصب پیش‌نیازها
```bash
pip install -r requirements_advanced_tts.txt
```

### 3. تست عملکرد
```bash
python test_avalai_tts.py
```

## 📝 **نحوه استفاده**

### استفاده مستقیم
```python
from advanced_tts import AdvancedTTS

tts = AdvancedTTS()

# تبدیل متن به صدا
audio_data = tts.text_to_speech_avalai(
    text="سلام، من دستیار هوشمند پزشکی هستم",
    voice="alloy",
    model="tts-1"
)

# ذخیره فایل
if audio_data:
    tts.save_audio_file(audio_data, "output.mp3")
```

### استفاده خودکار (توصیه شده)
```python
# سیستم خودکار بهترین ارائه‌دهنده را انتخاب می‌کند
audio_data = tts.synthesize_speech("متن فارسی")
```

## 🎛️ **تنظیمات پیشرفته**

### پارامترهای درخواست
```python
data = {
    "model": "tts-1",                    # مدل TTS
    "input": "متن فارسی",                # متن ورودی
    "voice": "alloy",                    # نوع صدا
    "response_format": "mp3",            # فرمت خروجی
    "speed": 1.0,                        # سرعت (0.25 تا 4.0)
    "instructions": "دستورالعمل اختیاری"  # فقط برای Gemini
}
```

### دستورالعمل‌های Gemini TTS
برای مدل‌های Gemini می‌توانید دستورالعمل‌های دقیق بدهید:
```python
instructions = """
با لهجه فارسی استاندارد و تلفظ صحیح کلمات پزشکی صحبت کنید.
سرعت متوسط و لحن دوستانه داشته باشید.
"""
```

## 🔍 **عیب‌یابی**

### مشکل: کلید API نامعتبر
```
❌ AvalAI API key not found
```
**راه‌حل:** کلید API را در فایل `.env` بررسی کنید.

### مشکل: خطای شبکه
```
❌ Connection error
```
**راه‌حل:** اتصال اینترنت و دسترسی به api.avalai.ir را بررسی کنید.

### مشکل: مدل در دسترس نیست
```
❌ Model not available
```
**راه‌حل:** از مدل‌های پشتیبانی شده استفاده کنید.

## 📊 **مقایسه با سایر سرویس‌ها**

| ویژگی | AvalAI | Azure | Google | OpenAI |
|--------|--------|-------|--------|--------|
| کیفیت فارسی | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| سرعت | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| هزینه | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ |
| پشتیبانی | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ |

## 🎯 **بهترین شیوه‌ها**

### 1. انتخاب مدل مناسب
- **tts-1**: برای استفاده عمومی
- **tts-1-hd**: برای کیفیت بالا
- **gemini-2.5-pro-preview-tts**: برای متن‌های پیچیده

### 2. انتخاب صدا مناسب
- **متن پزشکی**: `alloy`, `sage`
- **متن عمومی**: `nova`, `shimmer`
- **متن رسمی**: `onyx`, `echo`

### 3. تنظیم سرعت
- **متن پزشکی**: 0.8-1.0 (آهسته‌تر برای وضوح)
- **متن عمومی**: 1.0-1.2 (طبیعی)
- **متن سریع**: 1.2-1.5 (سریع‌تر)

## 🆘 **پشتیبانی**

اگر مشکلی دارید:
1. فایل `test_avalai_tts.py` را اجرا کنید
2. لاگ‌های خطا را بررسی کنید
3. مستندات رسمی آولای را مطالعه کنید
4. با پشتیبانی آولای تماس بگیرید

## 📞 **اطلاعات تماس**

- **وب‌سایت**: https://avalai.ir
- **مستندات**: https://docs.avalai.ir
- **API**: https://api.avalai.ir/v1 