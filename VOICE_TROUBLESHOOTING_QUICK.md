# راهنمای سریع حل مشکل پخش صدا

## 🚨 مشکل: هیچ صدایی پخش نمی‌شود

### مرحله 1: عیب‌یابی خودکار
1. روی دکمه 🐛 (Debug) کلیک کنید
2. روی "عیب‌یابی کامل صدا" کلیک کنید
3. نتایج را بررسی کنید

### مرحله 2: راه‌حل‌های سریع

#### ✅ راه‌حل 1: فعال‌سازی صدا
```
1. روی دکمه "🔊 Test Audio Now" کلیک کنید
2. اگر صدا پخش شد، مشکل حل شده
3. اگر نه، به مرحله بعد بروید
```

#### ✅ راه‌حل 2: بررسی مرورگر
```
✅ Chrome (توصیه شده)
✅ Edge
⚠️ Firefox (ممکن است مشکل داشته باشد)
❌ Safari (پشتیبانی محدود)
❌ Internet Explorer (پشتیبانی نشده)
```

#### ✅ راه‌حل 3: بررسی تنظیمات سیستم
```
1. مطمئن شوید که صدا در سیستم فعال است
2. Volume را چک کنید
3. Speakers/Headphones را بررسی کنید
4. از Mute نبودن مطمئن شوید
```

#### ✅ راه‌حل 4: اجازه پخش صدا
```
1. در نوار آدرس مرورگر، روی آیکون قفل کلیک کنید
2. مطمئن شوید که "Sound" مجاز است
3. یا روی "Allow" کلیک کنید
```

### مرحله 3: راه‌حل‌های پیشرفته

#### مشکل: Audio Context Suspended
```
راه‌حل: روی هر دکمه صدا کلیک کنید تا audio context فعال شود
```

#### مشکل: No Persian Voices
```
راه‌حل: سیستم از صداهای موجود استفاده می‌کند
```

#### مشکل: TTS API Failed
```
راه‌حل: از Browser TTS استفاده کنید (پشتیبان)
```

### مرحله 4: تست‌های مختلف

#### تست 1: صداهای انگلیسی
```
اگر صداهای انگلیسی کار می‌کند، مشکل از فارسی نیست
```

#### تست 2: Browser TTS
```
اگر Browser TTS کار نمی‌کند، مشکل از مرورگر است
```

#### تست 3: Advanced TTS
```
اگر Advanced TTS کار نمی‌کند، مشکل از API است
```

## 🔧 کدهای عیب‌یابی

### در Console مرورگر اجرا کنید:
```javascript
// تست 1: بررسی پشتیبانی
console.log('Speech Synthesis:', 'speechSynthesis' in window);
console.log('Audio API:', 'Audio' in window);

// تست 2: بررسی صداها
const voices = speechSynthesis.getVoices();
console.log('Available voices:', voices.length);

// تست 3: تست ساده
const utterance = new SpeechSynthesisUtterance("Hello");
speechSynthesis.speak(utterance);
```

### تست Audio Context:
```javascript
// تست Audio Context
const audioContext = new (window.AudioContext || window.webkitAudioContext)();
console.log('Audio Context State:', audioContext.state);

if (audioContext.state === 'suspended') {
    audioContext.resume().then(() => {
        console.log('Audio Context resumed');
    });
}
```

## 📋 چک‌لیست عیب‌یابی

- [ ] مرورگر Chrome یا Edge استفاده می‌کنم
- [ ] صدا در سیستم فعال است
- [ ] Volume مناسب است
- [ ] اجازه پخش صدا داده‌ام
- [ ] روی دکمه تست کلیک کرده‌ام
- [ ] Audio Context فعال شده
- [ ] صداهای موجود بررسی شده‌اند

## 🆘 اگر هیچ‌کدام کار نکرد

1. **مرورگر را عوض کنید** (Chrome توصیه می‌شود)
2. **سیستم را restart کنید**
3. **صداهای سیستم را reinstall کنید**
4. **با پشتیبانی تماس بگیرید**

## 📞 اطلاعات برای پشتیبانی

وقتی با پشتیبانی تماس می‌گیرید، این اطلاعات را آماده کنید:

```
مرورگر: [نام و نسخه]
سیستم عامل: [Windows/Mac/Linux]
خطاهای Console: [کپی کنید]
نتایج عیب‌یابی: [اسکرین‌شات بگیرید]
```

## 🎯 نکات مهم

1. **User Interaction**: مرورگرها برای پخش صدا نیاز به تعامل کاربر دارند
2. **HTTPS**: در برخی مرورگرها، صدا فقط در HTTPS کار می‌کند
3. **Extensions**: افزونه‌های مرورگر ممکن است صدا را مسدود کنند
4. **Firewall**: فایروال ممکن است صدا را مسدود کند 